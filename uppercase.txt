#include <stdio.h>
#include <ctype.h>

int main()
 {
    int i=0;
    char str[1000];
    fgets(str, sizeof(str), stdin);
    for(int i = 0; str[i] != '\0'; i++)
 {
        if(isupper(str[i]))
 {
            printf("%c", str[i]);
        }
    }
i++;
}